name : Basic CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: checkout code
      uses: actions/checkout@v2
    - name: say hello
      run: echo "Hello, This is Github Actions running!"
